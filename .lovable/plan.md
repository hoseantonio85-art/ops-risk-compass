## Что меняем

### 1. `AttentionZone.tsx` — убрать «AI» из подсказок
- Удалить мини-лейбл `AI` (`<span>AI</span>`) над текстом подсказки в виджетах «Зона внимания».
- Иконку Sparkles и мягкий gradient-фон оставить — они задают «AI-вид» без слова.
- Скорректировать вёрстку: текст `card.hint` идёт сразу справа от иконки, по центру по вертикали.

### 2. `AISummary.tsx` — уменьшить текст и сделать сворачиваемым
- Основной текст («Ситуация требует внимания…») уменьшить с `text-base lg:text-lg` до `text-sm lg:text-base`.
- Преобразовать баннер в управляемый аккордеон:
  - Добавить локальный `useState` `expanded` (по умолчанию `true`).
  - В шапке (рядом с заголовком «AI-саммари · ситуация на сейчас») добавить кнопку-чевшрон (`ChevronDown`/`ChevronUp` из lucide-react) для сворачивания. Вся шапка кликабельна.
  - В свёрнутом виде показывать только заголовок + одну строку короткого резюме («Лимит 82%, 4 просроченные меры — требует внимания»). Блоки с p-текстом и «Рекомендуем начать с» скрыты.
  - В развёрнутом виде — текущее содержимое.
  - Анимация через tailwind-классы (`grid-rows-[1fr]/[0fr]` или `max-h` transition), чтобы соответствовать стилистике проекта.

### 3. Аналитика — карта рисков 2/3 + вклад в утилизацию 1/3
- В `Index.tsx` убрать отдельную секцию `<ContributionWidget />` после `<Analytics />`.
- В `Analytics.tsx`:
  - Изменить grid с `grid-cols-1` на `lg:grid-cols-3`.
  - `<RiskHeatmap />` занимает `lg:col-span-2`, `<ContributionWidget />` — `lg:col-span-1`. Подзаголовок секции «Что формирует текущую картину» оставляем общим.
- В `ContributionWidget.tsx`:
  - Убрать внешнюю обёртку `section` с заголовком «Вклад в утилизацию» и `p`-описанием — теперь компонент рендерит только белую карточку (`surface-card`), как `RiskHeatmap`.
  - Внутри карточки сверху: заголовок `Вклад в утилизацию` (h3, как у `RiskHeatmap`) + краткая подпись справа (например, «доля от общих потерь»). Под ним — описание мелким текстом («Что больше влияет на лимит — клик по сегменту…»).
  - Убрать donut-график. Заменить на горизонтальную многосегментную полосу (как на референсе): один div с flex, внутри сегменты с шириной `flex: pct` и `backgroundColor` из `d.color`. Скруглённые края у первой/последней. Ховер на сегменте — небольшое затемнение, клик — открывает дровер. Подсветка синхронизирована с ховером строки списка ниже.
  - Под полосой — компактная легенда (точка + имя), как сейчас в списке, но в одну колонку. Toggle «По сценариям / По рискам» оставляем — переносим над полосой.
  - Список сегментов с `mln` и `%` показываем под полосой (как сейчас), один столбец, без donut.
  - Layout внутри карточки: `flex flex-col`, без двухколоночной сетки — теперь компактно по вертикали, чтобы влезать в 1/3 ширины.
  - Дровер с детализацией оставляем без изменений.

## Технические детали

- Файлы: `src/components/oprisk/AttentionZone.tsx`, `src/components/oprisk/AISummary.tsx`, `src/components/oprisk/ContributionWidget.tsx`, `src/components/oprisk/Analytics.tsx`, `src/pages/Index.tsx`.
- Цвета — только из существующих токенов (`--chart-1..6`, `--secondary`, `--primary`, etc.). Новых HSL не вводим.
- Recharts `BarChart`/`Donut` SVG в основном виджете больше не нужен, но импорты для дровера (`BarChart`, `Bar`, `XAxis`, `Tooltip`, `Cell`, `ResponsiveContainer`) сохраняем — они используются внутри `Sheet`.
- На узкой ширине (1/3 колонки на lg) — проверить, что названия сегментов не вылезают: используем `truncate` (уже есть).